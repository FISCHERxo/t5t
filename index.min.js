(leagues=["PL","BL1","PD","SA","FL1"]).forEach(t=>{fetch(`./standings/${t}.json`).then(t=>{if(!t.ok)throw Error("Network response was not ok");return t.json()}).then(e=>{let r=document.querySelector(`#${t} tbody`),n=e.standings[0].table;n.forEach(t=>{let e=document.createElement("tr");e.innerHTML=`
                    <td>${t.position}</td>
                    <td>
                        <img src="${t.team.crest}" alt="${t.team.name} crest" width="20"> 
                        ${t.team.name}
                    </td>
                    <td>${t.points}</td>
                    <td>${t.won} - ${t.draw} - ${t.lost}</td>
                    <td>${t.playedGames}</td>
            `,r.appendChild(e)})}).catch(t=>{console.error("Error fetching the JSON file:",t)})});